## setup_tool 模块说明

### 模块简介
该模块用于演示 `setup.bat` 的进度显示效果，不会执行真实安装，仅用于展示进度条、步骤状态和最终汇总信息。

### 代码位置
- `src/setup_tool/main.py`

### 运行方式
在项目根目录下执行：

```bash
python src/setup_tool/main.py

### 功能说明
运行后可以看到以下内容：

当前步骤编号
进度百分比
已用时间
预计剩余时间
最终汇总信息

### 适用场景
适合用于学习命令行进度展示、安装流程可视化和简单状态反馈设计。